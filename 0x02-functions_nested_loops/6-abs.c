#include "main.h"

/**
 * _abs - Returns the absolute value of the parameter
 * @n: The number in question
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
